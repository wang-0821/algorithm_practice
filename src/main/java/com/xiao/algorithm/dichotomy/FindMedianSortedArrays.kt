package com.xiao.algorithm.dichotomy

/**
 * leetcode 4
 *
 * 寻找两个有序数组的中位数
 *
 * 要求时间复杂度为O(log(m + n)) 那就需要使用二分法。
 * 如果两个数组的元素数量和 m + n 为奇数，那么中位数为两者合并后的 (a((m + n) / 2 - 1) + a((m + n) / 2)) / 2
 *
 * @author lix wang
 */
class FindMedianSortedArrays {
    fun findMedianSortedArrays(nums1: IntArray, nums2: IntArray): Double {
        return 0.0
    }
}